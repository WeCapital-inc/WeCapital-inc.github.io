---
title: ブログはじめました
description: はじめました
slug: start-blog
authors:
  - daichi.ogura
tags: [blog]
hide_table_of_contents: false
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Mermaid from "@theme/Mermaid";

# title

## overview

## diagram

<Tabs>
  <TabItem value="apple" label="Apple" default>
    ```mermaid
erDiagram
  User {
      int id PK
      string name
      string email
  }
  Invester {
      int id PK
      int user_id FK
      string investment_details
  }
  AdminUser {
      int id PK
      int user_id FK
      string admin_role
  }

User ||--o| Invester : "1 対 1"
User ||--o| AdminUser : "1 対 1"

`````
  </TabItem>
  <TabItem value="orange" label="Orange">
    ```
    erDiagram
      User {
          int id PK
          string name
          string email
      }
      Invester {
          int id PK
          int user_id FK
          string investment_details
      }
      AdminUser {
          int id PK
          int user_id FK
          string admin_role
      }

      User ||--o| Invester : "1対1"
      User ||--o| AdminUser : "1対1"
    ````
  </TabItem>
</Tabs>

<!-- truncate -->

<Tabs>
  <TabItem value="apple" label="Apple" default>
```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```
</TabItem>
<TabItem value="banana" label="Banana" >
```jsx


-```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
-```-

```
</TabItem>

</Tabs>

## graph

<Mermaid
  value={`graph TD;
    A-->B;
    A-->C;
    B-->D;
    C-->D;`}
/>

`aaaaaaaaa`

## code block

```python
import A

test = "a"
```

<Tabs>
  <TabItem value="apple" label="Apple" default>
    This is an apple 🍎
  </TabItem>
  <TabItem value="orange" label="Orange">
    This is an orange 🍊
  </TabItem>
  <TabItem value="banana" label="Banana">
    This is a banana 🍌
  </TabItem>
</Tabs>

:::note

Some **content** with _Markdown_ `syntax`. Check [this `api`](#).

:::

:::tip

Some **content** with _Markdown_ `syntax`. Check [this `api`](#).

:::

`````
